#include <iostream>
#include <cmath>
#include <cstdio>
#include <vector>

using namespace std;

int main() 
{
    int n,q;
    cin >> n >> q;

    vector<vector < int > > MAP = new vector<vector <int > >();

    return 0;
}